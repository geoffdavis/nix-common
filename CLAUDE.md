See @AGENTS.md for this repo's conventions, build/lint commands, and the
module contract. Before adding a shared module, read
@docs/module-contract.md and start from `templates/home-module`
(`task new:module -- <name>`).
